package ThirdDay;

public class Scenrio {
    public static void main(String[] args) {
        // how to reverse a string ?
        String carName = "Java";
        String reversed = "";
        for (int i = carName.length()-1; i >=0 ; i--) {
            reversed = reversed  + carName.charAt(i);
        }
        // Palindrome h k nhi?

    }
}
